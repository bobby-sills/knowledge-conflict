# Pilot study dependencies.
#
# Colab already ships torch built against its own CUDA; do NOT pin torch here or
# you will trigger a multi-GB reinstall and a runtime restart. The GRD repo pins
# torch==2.5.0 / transformers==4.51.3; we only borrow its pure-logits method
# classes and its string-normalisation metrics, neither of which is version
# sensitive, so we run on whatever Colab provides and record the versions in the
# run manifest instead. See DECISIONS.md ("dependency pinning").
transformers>=4.44
accelerate>=0.33
datasets>=2.20
huggingface_hub>=0.24
pandas>=2.0
pyarrow>=15.0
numpy>=1.26
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
tqdm>=4.66
sentencepiece
